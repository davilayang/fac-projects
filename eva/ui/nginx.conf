server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # /auth routing is handled by Traefik before requests reach this container.
    # React Router — all unknown paths fall back to index.html.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
